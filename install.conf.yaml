- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.antigen:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.zshenv:
    ~/.zshrc:

- shell:
        - [git submodule update --init --recursive, Installing submodules]
