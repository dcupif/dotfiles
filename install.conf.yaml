- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.antigen:
    ~/.bashrc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.zshrc:

- shell:
        - [git submodule update --init --recursive, Installing submodules]
